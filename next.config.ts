// Next.js configuration for the ClearDoc app.

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Intentionally minimal: project scaffolding only.
};

export default nextConfig;

